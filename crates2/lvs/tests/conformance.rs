// ponytail: the LVS conformance binary consumed the `gdsverify` engine crate
// (`use gdsverify::*`), which does not exist in crates2 yet. Re-add when the
// engine crate lands. All unit tests live in the crate itself.
