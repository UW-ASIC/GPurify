[package]
name = "gpurify-ingest"
version = "0.1.0"
edition = "2021"

[dependencies]
gpurify-geom = { workspace = true }
thiserror = { workspace = true }
flate2 = { workspace = true }
# `parse_deck` and `parse_intent` read JSON — the deck and the design intent are
# the only two text formats in `ingest` that are not a stream of records.
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
gpurify-testgen = { workspace = true }
gpurify-export = { workspace = true }

[lints]
workspace = true
