[package]
name = "gpurify-units"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
gpurify-testgen = { workspace = true }
# Not a new dependency: `serde_json` is already in `[workspace.dependencies]`
# and already in the lock via `gpurify-export`. It is here because `serde`
# alone is a data model with no format, so `Qty`'s two impls cannot be
# exercised without one concrete serialiser.
serde_json = { workspace = true }

[lints]
workspace = true
