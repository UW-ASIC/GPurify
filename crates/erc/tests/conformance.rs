// ponytail: re-add when engine crate lands — the ERC conformance binary drives
// the whole suite through `use gdsverify::*` (the `engine` crate), which does
// not exist in crates2. Every unit test lives in the crate itself and still runs.
